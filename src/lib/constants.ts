import type { GameServer, ServerTemplate } from './types';

export const APP_NAME = "Velocity Manager";

export const MOCK_SERVERS: GameServer[] = [];

export const MOCK_TEMPLATES: ServerTemplate[] = [];
